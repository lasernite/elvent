

<% if user_signed_in? %>
<h1>Thanks for signing up, <%= @user.name %>!</h1>
    <p>Thanks for joining and have a great day! Our elves are working to find some awesome events coming your way!</p>

<center>
   <%= link_to("Logout", destroy_user_session_path, :method => :delete, :class => "bottom_button") %>
   <%= link_to('Privacy Policy', 'privacy_policy', :target => '_blank', :class => "bottom_button") %>
   <%= link_to('Terms', 'terms', :target => '_blank', :class => "bottom_button") %>
   <%= link_to('Support', 'support', :target => '_blank', :class => "bottom_button") %>
</center>

<%# postfooter.html was here %>



<!-- 	<%= @response %>
	<%= link_to("Logout", destroy_user_session_path, :method => :delete) %>

	<% @local_top_12.each do |event| %>
		<%= image_tag event.image_url, :class => "event_image" %>
		<br>
		<br>
		<a href="https://facebook.com/events/<%= event.fb_id %>" target="_blank" >
			<%= event.name %>
		</a>

		<br>
		<%= event.start_time.to_date %>
		<%= event.start_time.to_time %>
		<%= event.longitude %>
		<%= event.latitude %>
		<br>
		<%= event.attending_count %>
		<br>
		<%= event.description %>
		<br>
	<% end %>
	<%= @just_string %>
	<%= @total_word_count %> -->
<% else %>
	<div class="home_page">
		<div class="welcome_one">Discover the Most Popular Events Near You!</div>
		<div class="welcome_two">Let our elves work for you ;)</div>
		<div class="fb_login_link">
			<%= link_to user_omniauth_authorize_path(:facebook) do  %>
				<div class="fb_login_button">
					<%= image_tag "facebook_logo.png", :class => 'facebook_logo' %>
					<div class="fb_login_text" %> 
						Sign up with Facebook
					</div>
				</div>
			<% end %>
		</div>
	</div>
<% end %>