<h1>Let our elves work for you!</h1>
<% if user_signed_in? %>
	<%= link_to("Logout", destroy_user_session_path, :method => :delete) %>
	<%= @cordit.first.city %>
	<%= @cordit.first.country %>
	<%= @cordit.first.state %>
	<%= @cord %>
	<%= @just_string %>
	<%= @total_word_count %>
<% else %>
	<%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>
<% end %>